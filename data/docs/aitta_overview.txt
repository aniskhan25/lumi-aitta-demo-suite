Aitta exposes model inference through an OpenAI-compatible chat completions interface.
Conversation history is sent explicitly through the messages field.
Direct OpenAI-compatible endpoints can use standard parameters such as temperature, top_p, max_completion_tokens, and n.
Streaming behavior should be validated against the specific endpoint path you use.
