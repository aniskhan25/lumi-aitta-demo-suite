Aitta exposes model inference through an OpenAI-compatible chat completions interface.
In the documented notebook flow, conversation history is sent explicitly through the messages field.
The same notebook notes that response streaming is not currently supported in that path.
It also shows parameters such as temperature, top_p, max_completion_tokens, and n for multiple completions.

